xml.instruct! :xml, :version=>"1.0", :encoding => "utf-8"

if @catched == true
  xml.response do
    xml.error "0"
    xml.message "Ping saved successfully."
  end
else
  xml.response do
    xml.error "1"
    xml.message "Ping saved successfully."
  end
end
